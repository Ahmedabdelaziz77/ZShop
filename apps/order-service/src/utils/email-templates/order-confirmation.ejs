<!DOCTYPE html>
<html>
  <head>
    <title>Zshop Order Confirmation</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style type="text/css">
      /* Base */
      body {
        margin: 0;
        padding: 0;
        min-width: 100%;
        font-family: Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        background-color: #fafafa;
        color: #222222;
      }

      a {
        color: #0070f3;
        text-decoration: none;
      }

      h1 {
        font-size: 24px;
        font-weight: 700;
        line-height: 1.25;
        margin-top: 0;
        margin-bottom: 15px;
        text-align: center;
      }

      h2 {
        font-size: 22px;
        font-weight: bold;
        color: #0070f3;
        margin-bottom: 10px;
      }

      p {
        margin-top: 0;
        margin-bottom: 16px;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
      }

      th,
      td {
        border: 1px solid #ddd;
        padding: 10px;
        text-align: left;
      }

      th {
        background-color: #f6f6f6;
      }

      /* Layout */
      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
      }

      .email-header {
        background-color: #0070f3;
        padding: 24px;
        color: #ffffff;
        text-align: center;
      }

      .email-body {
        padding: 24px;
        background-color: #ffffff;
      }

      .email-footer {
        background-color: #f6f6f6;
        padding: 24px;
        text-align: center;
        font-size: 12px;
        color: #555;
      }

      .total {
        font-size: 18px;
        font-weight: bold;
        text-align: right;
        margin-top: 10px;
      }

      .btn {
        display: inline-block;
        padding: 12px 20px;
        background-color: #0070f3;
        color: #fff;
        border-radius: 5px;
        text-decoration: none;
        font-weight: bold;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="email-header">
        <h1>Order Confirmation</h1>
      </div>

      <div class="email-body">
        <p>Hello <%= name %>,</p>
        <p>
          Thank you for your purchase! Your order has been successfully placed.
          Here’s a summary of your items:
        </p>

        <table>
          <thead>
            <tr>
              <th>Item</th>
              <th>Qty</th>
              <th>Price</th>
            </tr>
          </thead>
          <tbody>
            <% cart.forEach(function(item) { %>
            <tr>
              <td><%= item.title %></td>
              <td><%= item.quantity %></td>
              <td>$<%= item.sale_price.toFixed(2) %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>

        <p class="total">Total: $<%= totalAmount.toFixed(2) %></p>

        <p><strong>Order Date:</strong> <%= new Date().toLocaleString() %></p>

        <p>You can track your order anytime using the link below:</p>

        <p style="text-align: center">
          <a href="<%= trackingUrl %>" class="btn">Track Your Order</a>
        </p>

        <p>
          If you have any questions, reply to this email and our team will be
          happy to help.
        </p>
      </div>

      <div class="email-footer">
        <p>Thank you for shopping with Zshop!</p>
        <p>
          Need help? Contact us at
          <a href="mailto:zozahmed770@gmail.com">zozahmed770@gmail.com</a>
        </p>
        <p>
          &copy; <%= new Date().getFullYear() %> Zshop. All rights reserved.
        </p>
      </div>
    </div>
  </body>
</html>
